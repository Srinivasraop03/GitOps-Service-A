apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitops-service-a
  namespace: gitops-service-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gitops-service-a
  template:
    metadata:
      labels:
        app: gitops-service-a
    spec:
      containers:
        - name: app
          image: srinivasraop03/gitops-service-a:sha-65172a8
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: gitops-service-a
  namespace: gitops-service-a
spec:
  selector:
    app: gitops-service-a
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
